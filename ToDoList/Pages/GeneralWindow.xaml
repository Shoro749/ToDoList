<Window x:Class="ToDoList.Pages.GeneralWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList.Pages"
        mc:Ignorable="d"
        Title="GeneralWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
            Background="#388E3C"  
            Padding="5">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                       Text="JUST DO LIST"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"/>

                <Button Grid.Column="1" 
                    Content="Add new list"
                    HorizontalAlignment="Left"
                    Margin="15,0,0,0"
                    Background="#4CAF50"
                    Foreground="White"
                    Height="30" Click="CreateNewList">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#388E3C"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <TextBox Grid.Column="2"
                 Width="150"
                 Height="30"
                 Margin="0,0,10,0"
                 VerticalAlignment="Center"/>

                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Log out"
                    Margin="0,0,5,0"
                    Background="#D32F2F"
                    Foreground="White"
                    Height="30" Click="LogOutClick"/>
                    <Button Content="Delete users"
                    Margin="0,0,10,0"
                    Background="#D32F2F"
                    Foreground="White"
                    Height="30" Click="DeleteUserClick"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
                Background="#2D2D30" 
                Padding="10">
                <StackPanel x:Name="sp_lists">

                    <!--<TextBlock Text="Jobs" 
                           Foreground="#AAAAAA" 
                           FontSize="16" 
                           Margin="0,10,0,0"/>
                <Separator Background="#444444" Margin="0,5,0,10"/>

                <TextBlock Text="Products" 
                           Foreground="#AAAAAA" 
                           FontSize="16"/>
                <Separator Background="#444444" Margin="0,5,0,10"/>-->

                </StackPanel>
            </Border>

            <Border Grid.Column="1" 
                Background="#1E1E1E" 
                Padding="20">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="Welcome! Your tasks are here." 
                           Foreground="#999999" 
                           FontSize="20"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Visible"/>

                    <DataGrid x:Name="dg_tasks"
                      Grid.Row="0"
                      AutoGenerateColumns="True"
                      Background="#2D2D30" 
                      Foreground="Black" 
                      BorderBrush="#4CAF50"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      Visibility="Hidden">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="5"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="Foreground" Value="Black"/>
                                <Setter Property="Background" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#4CAF50"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.CellStyle>
                    </DataGrid>

                    <StackPanel Grid.Row="1" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right"
                        Margin="0,15,0,0">

                        <Button Content="Add"
                            Width="80"
                            Height="30"
                            Background="#4CAF50"
                            Foreground="White"
                            Margin="0,0,10,0"
                            Click="AddTaskClick">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#388E3C"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Content="Update"
                            Width="80"
                            Height="30"
                            Background="#3F51B5"
                            Foreground="White"
                            Margin="0,0,10,0"
                            Click="UpdateTaskClick">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Background" Value="#3F51B5"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#303F9F"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Content="Delete"
                            Width="80"
                            Height="30"
                            Background="#D32F2F"
                            Foreground="White"
                            Click="DeleteTaskClick">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#C0392B"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>

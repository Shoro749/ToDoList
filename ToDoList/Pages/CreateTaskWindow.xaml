<Window x:Class="ToDoList.Pages.TaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList.Pages"
        mc:Ignorable="d"
        Title="CreateTaskWindow" Height="350" Width="450"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize"
        Background="#2D2D30">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="15"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Description:" Foreground="#AAAAAA" Margin="0,0,0,5"/>
        <TextBox x:Name="tbDescription" 
                 Grid.Row="1" 
                 Height="70" 
                 TextWrapping="Wrap"
                 Background="#3E3E42" 
                 Foreground="White"
                 BorderBrush="#4CAF50"
                 Padding="5"/>

        <TextBlock Grid.Row="3" Text="Status:" Foreground="#AAAAAA" Margin="0,0,0,5"/>
        <ComboBox x:Name="cbStatus"
          Grid.Row="4"
          Height="30"
          Background="White" 
          Foreground="Black" 
          BorderBrush="#4CAF50">
            <ComboBoxItem Content="To Do 📋"/>
            <ComboBoxItem Content="In Progress 🚀"/>
            <ComboBoxItem Content="Done ✅"/>

            <ComboBox.ItemContainerStyle>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="Background" Value="White"/>
                </Style>
            </ComboBox.ItemContainerStyle>
        </ComboBox>

        <TextBlock Grid.Row="6" Text="Due Date:" Foreground="#AAAAAA" Margin="0,0,0,5"/>
        <DatePicker x:Name="dpDate"
                    Grid.Row="7"
                    Height="30"
                    Background="#3E3E42"
                    Foreground="Black"
                    BorderBrush="#4CAF50"/>

        <StackPanel Grid.Row="9" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">

            <Button Content="Save"
                    Width="80" Height="30" Margin="0,0,10,0"
                    Background="#4CAF50" Foreground="White"
                    Click="SaveButton_Click"/>

            <Button Content="Cancel"
                    Width="80" Height="30"
                    Background="#555555" Foreground="White"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
